---
phase: 02-triage-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AIAgents.Core/Configuration/DataverseOptions.cs
  - src/AIAgents.Functions/Models/ErrorTrackingRecord.cs
  - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs
  - src/AIAgents.Functions/Models/ErrorTriageDecision.cs
  - src/AIAgents.Functions/Services/IErrorTriageService.cs
  - src/AIAgents.Functions/Services/ErrorTriageService.cs
  - src/AIAgents.Functions/Program.cs
  - src/AIAgents.Functions.Tests/Services/ErrorTriageServiceTests.cs
autonomous: true
requirements:
  - TRIAGE-01
  - DETECT-05
  - DETECT-07
  - DETECT-08
  - DETECT-09
user_setup: []
must_haves:
  truths:
    - "The triage service applies a deterministic code-first funnel before any AI classification path exists."
    - "Cascade errors and known noise patterns are stopped before downstream classification or Bug creation."
    - "Tracked errors retain threshold and rolling-occurrence context that later plans can reuse."
  artifacts:
    - path: src/AIAgents.Functions/Services/ErrorTriageService.cs
      provides: "Code-first triage skeleton with threshold, severity, and noise handling"
      min_lines: 140
      contains: "EvaluateAsync"
    - path: src/AIAgents.Functions/Models/ErrorTriageDecision.cs
      provides: "Triage decision contract for later AI and Bug plans"
      contains: "ReadyForClassification"
    - path: src/AIAgents.Core/Configuration/DataverseOptions.cs
      provides: "Threshold and occurrence-history configuration"
      contains: "DefaultBugOccurrenceThreshold"
  key_links:
    - from: src/AIAgents.Functions/Services/ErrorTriageService.cs
      to: src/AIAgents.Functions/Services/IErrorTrackingService.cs
      via: "tracked-error lookup and persistence"
      pattern: "IErrorTrackingService"
    - from: src/AIAgents.Functions/Services/ErrorTriageService.cs
      to: src/AIAgents.Core/Interfaces/IErrorFingerprintService.cs
      via: "fingerprint generation"
      pattern: "IErrorFingerprintService"
    - from: src/AIAgents.Functions/Program.cs
      to: src/AIAgents.Functions/Services/ErrorTriageService.cs
      via: "service registration"
      pattern: "AddSingleton<IErrorTriageService, ErrorTriageService>"
---

<objective>
Implement the code-first triage skeleton.

Purpose: Establish the deterministic funnel that stops disabled runs, cascade errors, noisy failures, and sub-threshold occurrences before AI is involved.
Output: Threshold-aware triage service, triage decision model, rolling occurrence tracking, registration, and tests for the code-first branches.
</objective>

<execution_context>
@C:/Users/toddp/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/toddp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-triage-orchestrator/02-RESEARCH.md
@.planning/phases/02-triage-orchestrator/02-VALIDATION.md
@src/AIAgents.Core/Configuration/DataverseOptions.cs
@src/AIAgents.Core/Models/PluginTraceLogEntry.cs
@src/AIAgents.Core/Services/ErrorFingerprintService.cs
@src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs
@src/AIAgents.Functions/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend monitor configuration and tracked-error storage for triage state</name>
  <files>src/AIAgents.Core/Configuration/DataverseOptions.cs, src/AIAgents.Functions/Models/ErrorTrackingRecord.cs, src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs</files>
  <read_first>src/AIAgents.Core/Configuration/DataverseOptions.cs, src/AIAgents.Functions/Models/ErrorTrackingRecord.cs, src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs, .planning/phases/02-triage-orchestrator/02-RESEARCH.md</read_first>
  <action>Extend `DataverseOptions` with `public int DefaultBugOccurrenceThreshold { get; init; } = 3;`, `public int OccurrenceHistoryLimit { get; init; } = 20;`, and `public Dictionary<string, int> PluginOccurrenceThresholds { get; init; } = new(StringComparer.OrdinalIgnoreCase);`. Extend `ErrorTrackingRecord` with `public string OccurrenceHistoryJson { get; set; } = "[]";`. Update `TableStorageErrorTrackingService` so `OccurrenceHistoryJson` is written to and read from the `TableEntity` under the exact column name `OccurrenceHistoryJson`, defaulting to `"[]"` when absent.</action>
  <verify>rg -n "DefaultBugOccurrenceThreshold|OccurrenceHistoryLimit|PluginOccurrenceThresholds|OccurrenceHistoryJson" src/AIAgents.Core src/AIAgents.Functions</verify>
  <acceptance_criteria>
    - src/AIAgents.Core/Configuration/DataverseOptions.cs contains `public int DefaultBugOccurrenceThreshold { get; init; } = 3;`
    - src/AIAgents.Core/Configuration/DataverseOptions.cs contains `public int OccurrenceHistoryLimit { get; init; } = 20;`
    - src/AIAgents.Core/Configuration/DataverseOptions.cs contains `public Dictionary<string, int> PluginOccurrenceThresholds`
    - src/AIAgents.Functions/Models/ErrorTrackingRecord.cs contains `public string OccurrenceHistoryJson { get; set; } = "[]";`
    - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs contains `OccurrenceHistoryJson`
  </acceptance_criteria>
  <done>The monitor configuration and tracked-error schema support thresholds and rolling occurrence history.</done>
</task>

<task type="auto">
  <name>Task 2: Add the code-first triage service and decision model</name>
  <files>src/AIAgents.Functions/Models/ErrorTriageDecision.cs, src/AIAgents.Functions/Services/IErrorTriageService.cs, src/AIAgents.Functions/Services/ErrorTriageService.cs, src/AIAgents.Functions/Program.cs</files>
  <read_first>src/AIAgents.Functions/Program.cs, src/AIAgents.Core/Models/PluginTraceLogEntry.cs, src/AIAgents.Core/Interfaces/IErrorFingerprintService.cs, src/AIAgents.Functions/Services/IErrorTrackingService.cs, src/AIAgents.Core/Configuration/DataverseOptions.cs, .planning/phases/02-triage-orchestrator/02-RESEARCH.md</read_first>
  <action>Create `ErrorTriageDecision` with exact outcome strings `SkippedDisabled`, `SkippedCascade`, `SkippedNoise`, `AwaitingThreshold`, and `ReadyForClassification`, plus properties `Fingerprint`, `NormalizedMessage`, `OccurrenceCount`, `RequiredOccurrences`, `Severity`, and `TrackingRecord`. Create `IErrorTriageService` with `Task<ErrorTriageDecision> EvaluateAsync(PluginTraceLogEntry entry, CancellationToken cancellationToken = default);`. Implement `ErrorTriageService` so `EvaluateAsync(...)` first returns `SkippedDisabled` when `DataverseOptions.IsConfigured` is false, then returns `SkippedCascade` when `entry.Depth.GetValueOrDefault() > 0`, then computes `messageText = entry.ExceptionDetails ?? entry.MessageBlock ?? string.Empty`, computes the fingerprint through `IErrorFingerprintService`, loads or creates a tracked row, appends the new `CreatedOnUtc` timestamp into `OccurrenceHistoryJson` and trims it to `OccurrenceHistoryLimit`, marks `Severity = "Synchronous"` when `entry.Mode == 0` and `Severity = "Asynchronous"` otherwise, short-circuits to `SkippedNoise` for case-insensitive message matches on `429 Too Many Requests`, `Rate limit is exceeded`, `Execution Timeout Expired`, or `SqlException: Timeout expired`, and returns `AwaitingThreshold` until the plugin-specific threshold from `PluginOccurrenceThresholds` or `DefaultBugOccurrenceThreshold` is met. Register `IErrorTriageService` in `Program.cs` with `services.AddSingleton<IErrorTriageService, ErrorTriageService>();`.</action>
  <verify>rg -n "SkippedDisabled|SkippedCascade|SkippedNoise|AwaitingThreshold|ReadyForClassification|429 Too Many Requests|Execution Timeout Expired|AddSingleton<IErrorTriageService, ErrorTriageService>" src/AIAgents.Functions</verify>
  <acceptance_criteria>
    - src/AIAgents.Functions/Models/ErrorTriageDecision.cs contains `ReadyForClassification`
    - src/AIAgents.Functions/Services/IErrorTriageService.cs contains `Task<ErrorTriageDecision> EvaluateAsync`
    - src/AIAgents.Functions/Services/ErrorTriageService.cs contains `entry.Depth.GetValueOrDefault() > 0`
    - src/AIAgents.Functions/Services/ErrorTriageService.cs contains `429 Too Many Requests`
    - src/AIAgents.Functions/Services/ErrorTriageService.cs contains `Execution Timeout Expired`
    - src/AIAgents.Functions/Program.cs contains `AddSingleton<IErrorTriageService, ErrorTriageService>();`
  </acceptance_criteria>
  <done>The code-first triage funnel exists, is registered, and produces an explicit decision contract for later plans.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for disabled, cascade, noise, threshold, and history behavior</name>
  <files>src/AIAgents.Functions.Tests/Services/ErrorTriageServiceTests.cs</files>
  <read_first>src/AIAgents.Functions.Tests/Services/TableStorageErrorTrackingServiceTests.cs, src/AIAgents.Functions/Services/ErrorTriageService.cs, src/AIAgents.Core.Tests/Services/ErrorFingerprintServiceTests.cs, .planning/phases/02-triage-orchestrator/02-VALIDATION.md</read_first>
  <action>Create `ErrorTriageServiceTests` with fakes or mocks for `IErrorTrackingService` and `IErrorFingerprintService`. Cover these exact scenarios: missing Dataverse config returns `SkippedDisabled`; `Depth = 1` returns `SkippedCascade`; a message containing `Execution Timeout Expired` returns `SkippedNoise` and stores `Classification = "NOISE"` on the tracked row; a plugin threshold configured through `PluginOccurrenceThresholds["Contoso.Plugins.AccountSync"] = 2` returns `AwaitingThreshold` on the first occurrence and `ReadyForClassification` on the second; and the stored `OccurrenceHistoryJson` is capped to the configured `OccurrenceHistoryLimit`.</action>
  <verify>dotnet test src/AIAgents.Functions.Tests/AIAgents.Functions.Tests.csproj --filter "FullyQualifiedName~ErrorTriageServiceTests"</verify>
  <acceptance_criteria>
    - src/AIAgents.Functions.Tests/Services/ErrorTriageServiceTests.cs contains `SkippedDisabled`
    - src/AIAgents.Functions.Tests/Services/ErrorTriageServiceTests.cs contains `SkippedCascade`
    - src/AIAgents.Functions.Tests/Services/ErrorTriageServiceTests.cs contains `SkippedNoise`
    - src/AIAgents.Functions.Tests/Services/ErrorTriageServiceTests.cs contains `ReadyForClassification`
    - src/AIAgents.Functions.Tests/Services/ErrorTriageServiceTests.cs contains `OccurrenceHistoryJson`
    - `dotnet test src/AIAgents.Functions.Tests/AIAgents.Functions.Tests.csproj --filter "FullyQualifiedName~ErrorTriageServiceTests"` exits 0
  </acceptance_criteria>
  <done>The code-first triage branches are protected by targeted unit tests before AI or Bug wiring is added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test src/AIAgents.Functions.Tests/AIAgents.Functions.Tests.csproj --filter "FullyQualifiedName~ErrorTriageServiceTests"` succeeds
- [ ] `dotnet build src/AIAgents.sln` succeeds
- [ ] `src/AIAgents.Functions/Services/ErrorTriageService.cs` contains the code-first outcome branches from this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The triage service enforces deterministic pre-AI filtering and threshold rules
- Rolling occurrence data is persisted for future trend and lifecycle work
  </success_criteria>

<output>
After completion, create `.planning/phases/02-triage-orchestrator/02-01-SUMMARY.md`
</output>
