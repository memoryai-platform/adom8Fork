---
phase: 01-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AIAgents.Functions/Models/ErrorTrackingRecord.cs
  - src/AIAgents.Functions/Services/IErrorTrackingService.cs
  - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs
  - src/AIAgents.Functions.Tests/Services/TableStorageErrorTrackingServiceTests.cs
autonomous: true
requirements:
  - DETECT-03
  - DETECT-06
user_setup: []
must_haves:
  truths:
    - "Tracked errors can be created and updated in Azure Table Storage without losing occurrence counts."
    - "The scan watermark survives restarts because it is stored outside function memory."
    - "Concurrent updates to the same tracked error retry safely instead of silently dropping changes."
  artifacts:
    - path: src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs
      provides: "ErrorTracking table CRUD, watermark persistence, and optimistic concurrency"
      min_lines: 120
      contains: "plugintracelog-watermark"
    - path: src/AIAgents.Functions/Models/ErrorTrackingRecord.cs
      provides: "Tracked error state model"
      contains: "OccurrenceCount"
    - path: src/AIAgents.Functions/Services/IErrorTrackingService.cs
      provides: "Tracking service contract for later triage phases"
      contains: "GetWatermarkAsync"
  key_links:
    - from: src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs
      to: ErrorTracking
      via: "TableClient initialization"
      pattern: "new TableClient\\(connectionString, TableName\\)"
    - from: src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs
      to: plugintracelog-watermark
      via: "meta entity row key"
      pattern: "plugintracelog-watermark"
---

<objective>
Implement ErrorTracking persistence in Azure Table Storage.

Purpose: Give the Dataverse monitor a durable record of fingerprints, counts, status metadata, and the last completed scan watermark.
Output: ErrorTracking record model, service contract, table-backed implementation, and Functions-layer storage tests.
</objective>

<execution_context>
@C:/Users/toddp/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/toddp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@src/AIAgents.Functions/Services/TableStorageActivityLogger.cs
@src/AIAgents.Functions/Services/CopilotDelegationService.cs
@src/AIAgents.Functions/Functions/HealthCheck.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the tracked-error model and service contract</name>
  <files>src/AIAgents.Functions/Models/ErrorTrackingRecord.cs, src/AIAgents.Functions/Services/IErrorTrackingService.cs</files>
  <read_first>src/AIAgents.Functions/Services/IActivityLogger.cs, src/AIAgents.Functions/Services/CopilotDelegationService.cs, .planning/phases/01-infrastructure-foundation/01-RESEARCH.md</read_first>
  <action>Create `ErrorTrackingRecord` in `src/AIAgents.Functions/Models/ErrorTrackingRecord.cs` with properties `PluginType`, `MessageName`, `PrimaryEntity`, `Fingerprint`, `NormalizedMessage`, `OccurrenceCount`, `FirstSeenUtc`, `LastSeenUtc`, `Status`, `Classification`, `WorkItemId`, `SuppressedUntilUtc`, `ResolvedAtUtc`, `ConsecutiveMissedWindows`, and `ETag`. Create `IErrorTrackingService` in `src/AIAgents.Functions/Services/IErrorTrackingService.cs` with explicit methods `GetAsync(string pluginType, string fingerprint, CancellationToken cancellationToken = default)`, `UpsertAsync(ErrorTrackingRecord record, ETag? expectedEtag = null, CancellationToken cancellationToken = default)`, `GetWatermarkAsync(CancellationToken cancellationToken = default)`, and `SetWatermarkAsync(DateTime watermarkUtc, ETag? expectedEtag = null, CancellationToken cancellationToken = default)`.</action>
  <verify>rg -n "OccurrenceCount|GetWatermarkAsync|SetWatermarkAsync|ETag" src/AIAgents.Functions</verify>
  <acceptance_criteria>
    - src/AIAgents.Functions/Models/ErrorTrackingRecord.cs contains `public int OccurrenceCount { get; set; }`
    - src/AIAgents.Functions/Models/ErrorTrackingRecord.cs contains `public ETag ETag { get; set; }`
    - src/AIAgents.Functions/Services/IErrorTrackingService.cs contains `Task<DateTime?> GetWatermarkAsync`
    - src/AIAgents.Functions/Services/IErrorTrackingService.cs contains `Task SetWatermarkAsync(DateTime watermarkUtc`
  </acceptance_criteria>
  <done>The Functions project exposes a durable tracked-error model and a storage contract that includes watermark operations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the ErrorTracking table service with watermark and optimistic concurrency</name>
  <files>src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs</files>
  <read_first>src/AIAgents.Functions/Services/TableStorageActivityLogger.cs, src/AIAgents.Functions/Services/CopilotDelegationService.cs, src/AIAgents.Functions/Models/ErrorTrackingRecord.cs, src/AIAgents.Functions/Services/IErrorTrackingService.cs, .planning/phases/01-infrastructure-foundation/01-RESEARCH.md</read_first>
  <action>Create `TableStorageErrorTrackingService` in `src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs`. Use `TableName = "ErrorTracking"`, create the table if missing, store error rows with `PartitionKey = pluginType.Trim().ToLowerInvariant()` and `RowKey = fingerprint`, and store the watermark row with `PartitionKey = "meta"` and `RowKey = "plugintracelog-watermark"`. Implement `UpsertAsync(...)` so new records are created and existing records are replaced with the caller's expected `ETag`. When `UpdateEntityAsync(...)` fails with HTTP 412, reload the current entity, merge `OccurrenceCount` and `LastSeenUtc`, and retry up to 3 total attempts. Add an internal seam such as an overload that accepts `TableClient` so the service is unit-testable without a live storage account.</action>
  <verify>rg -n "ErrorTracking|plugintracelog-watermark|UpdateEntityAsync|Status == 412|TableUpdateMode.Replace" src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs</verify>
  <acceptance_criteria>
    - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs contains `TableName = "ErrorTracking"`
    - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs contains `plugintracelog-watermark`
    - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs contains `UpdateEntityAsync`
    - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs contains `RequestFailedException ex when (ex.Status == 412)`
    - src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs contains `TableUpdateMode.Replace`
  </acceptance_criteria>
  <done>The table service can create, update, and reload tracked-error records and persist the scan watermark with explicit concurrency handling.</done>
</task>

<task type="auto">
  <name>Task 3: Add Functions tests for record persistence and watermark behavior</name>
  <files>src/AIAgents.Functions.Tests/Services/TableStorageErrorTrackingServiceTests.cs</files>
  <read_first>src/AIAgents.Functions.Tests/Functions/HealthCheckTests.cs, src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs, src/AIAgents.Functions/Services/CopilotDelegationService.cs</read_first>
  <action>Create `src/AIAgents.Functions.Tests/Services/TableStorageErrorTrackingServiceTests.cs` with tests that cover: creating a new tracked error; updating occurrence count and `LastSeenUtc`; persisting and reading the watermark row; and retrying on an initial 412 concurrency failure before succeeding. Use the test seam from Task 2 instead of a live `AzureWebJobsStorage` connection.</action>
  <verify>dotnet test src/AIAgents.Functions.Tests/AIAgents.Functions.Tests.csproj --filter "FullyQualifiedName~TableStorageErrorTrackingServiceTests"</verify>
  <acceptance_criteria>
    - src/AIAgents.Functions.Tests/Services/TableStorageErrorTrackingServiceTests.cs contains `public sealed class TableStorageErrorTrackingServiceTests`
    - src/AIAgents.Functions.Tests/Services/TableStorageErrorTrackingServiceTests.cs contains `GetWatermarkAsync`
    - src/AIAgents.Functions.Tests/Services/TableStorageErrorTrackingServiceTests.cs contains `412`
    - `dotnet test src/AIAgents.Functions.Tests/AIAgents.Functions.Tests.csproj --filter "FullyQualifiedName~TableStorageErrorTrackingServiceTests"` exits 0
  </acceptance_criteria>
  <done>The storage implementation is backed by automated tests for CRUD, watermark, and concurrency behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test src/AIAgents.Functions.Tests/AIAgents.Functions.Tests.csproj --filter "FullyQualifiedName~TableStorageErrorTrackingServiceTests"` succeeds
- [ ] `dotnet build src/AIAgents.sln` succeeds
- [ ] `src/AIAgents.Functions/Services/TableStorageErrorTrackingService.cs` contains the watermark row key and optimistic concurrency branch from this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- ErrorTracking persistence supports record CRUD and watermark persistence
- Optimistic concurrency behavior is explicitly implemented and tested
  </success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md`
</output>
