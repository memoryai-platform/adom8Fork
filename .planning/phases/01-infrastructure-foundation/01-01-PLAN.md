---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AIAgents.Core/AIAgents.Core.csproj
  - src/AIAgents.Core/Configuration/DataverseOptions.cs
  - src/AIAgents.Core/Interfaces/IDataverseClient.cs
  - src/AIAgents.Core/Models/PluginTraceLogEntry.cs
  - src/AIAgents.Core/Services/DataverseClient.cs
  - src/AIAgents.Core.Tests/Services/DataverseClientTests.cs
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - DETECT-01
user_setup: []
must_haves:
  truths:
    - "Configured Dataverse services can acquire an app-only token without changing unrelated startup paths."
    - "Configured Dataverse queries return PluginTraceLog rows with non-null exception details in ascending createdon order."
    - "Paged Dataverse responses are followed until all matching PluginTraceLog rows are collected."
  artifacts:
    - path: src/AIAgents.Core/Services/DataverseClient.cs
      provides: "MSAL-backed Dataverse Web API client with paging and retry behavior"
      min_lines: 80
      contains: "AcquireTokenForClient"
    - path: src/AIAgents.Core/Configuration/DataverseOptions.cs
      provides: "Dataverse monitor configuration contract"
      contains: "SectionName = \"Dataverse\""
    - path: src/AIAgents.Core/Models/PluginTraceLogEntry.cs
      provides: "Typed PluginTraceLog payload model"
      contains: "ExceptionDetails"
  key_links:
    - from: src/AIAgents.Core/Services/DataverseClient.cs
      to: Dataverse
      via: "named HttpClient lookup"
      pattern: "CreateClient\\(\"Dataverse\"\\)"
    - from: src/AIAgents.Core/Services/DataverseClient.cs
      to: src/AIAgents.Core/Configuration/DataverseOptions.cs
      via: "IOptions-based configuration"
      pattern: "IOptions<DataverseOptions>"
---

<objective>
Implement the Dataverse client foundation in the Core project.

Purpose: Give later phases a stable, testable API for authenticating to Dataverse and reading PluginTraceLog records.
Output: Dataverse configuration/options, client interface, typed PluginTraceLog model, Dataverse Web API client, and Core-layer tests.
</objective>

<execution_context>
@C:/Users/toddp/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/toddp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@src/AIAgents.Functions/Program.cs
@src/AIAgents.Core/Configuration/AIOptions.cs
@src/AIAgents.Core/Interfaces/IAzureDevOpsClient.cs
@src/AIAgents.Core/Services/AIClient.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dataverse configuration and client contracts</name>
  <files>src/AIAgents.Core/Configuration/DataverseOptions.cs, src/AIAgents.Core/Interfaces/IDataverseClient.cs, src/AIAgents.Core/Models/PluginTraceLogEntry.cs</files>
  <read_first>src/AIAgents.Core/Configuration/AIOptions.cs, src/AIAgents.Core/Interfaces/IAzureDevOpsClient.cs, .planning/phases/01-infrastructure-foundation/01-RESEARCH.md</read_first>
  <action>Create `DataverseOptions` in `src/AIAgents.Core/Configuration/DataverseOptions.cs` with `public const string SectionName = "Dataverse";`, string properties `BaseUrl`, `TenantId`, `ClientId`, `ClientSecret`, `MonitorSchedule = "0 */15 * * * *"`, `PluginTraceLogPageSize = 250`, and a computed `IsConfigured` property that returns true only when the four connection values are non-empty. Create `IDataverseClient` in `src/AIAgents.Core/Interfaces/IDataverseClient.cs` with one method signature: `Task<IReadOnlyList<PluginTraceLogEntry>> GetPluginTraceLogsAsync(DateTime? createdAfterUtc = null, CancellationToken cancellationToken = default);`. Create `PluginTraceLogEntry` in `src/AIAgents.Core/Models/PluginTraceLogEntry.cs` with init-only properties for `PluginTraceLogId`, `TypeName`, `MessageName`, `PrimaryEntity`, `Mode`, `Depth`, `CreatedOnUtc`, `ExceptionDetails`, and `MessageBlock`.</action>
  <verify>rg -n "SectionName = \"Dataverse\"|MonitorSchedule|GetPluginTraceLogsAsync|ExceptionDetails" src/AIAgents.Core</verify>
  <acceptance_criteria>
    - src/AIAgents.Core/Configuration/DataverseOptions.cs contains `public const string SectionName = "Dataverse";`
    - src/AIAgents.Core/Configuration/DataverseOptions.cs contains `public string MonitorSchedule { get; init; } = "0 */15 * * * *";`
    - src/AIAgents.Core/Interfaces/IDataverseClient.cs contains `Task<IReadOnlyList<PluginTraceLogEntry>> GetPluginTraceLogsAsync`
    - src/AIAgents.Core/Models/PluginTraceLogEntry.cs contains `public string? ExceptionDetails { get; init; }`
  </acceptance_criteria>
  <done>The Core project exposes a concrete Dataverse options contract, a Dataverse client interface, and a typed PluginTraceLog model.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Dataverse Web API client with auth, paging, and retry</name>
  <files>src/AIAgents.Core/Services/DataverseClient.cs</files>
  <read_first>src/AIAgents.Core/Services/AIClient.cs, src/AIAgents.Core/Configuration/DataverseOptions.cs, src/AIAgents.Core/Interfaces/IDataverseClient.cs, src/AIAgents.Core/Models/PluginTraceLogEntry.cs, .planning/phases/01-infrastructure-foundation/01-RESEARCH.md</read_first>
  <action>Create `DataverseClient` in `src/AIAgents.Core/Services/DataverseClient.cs` that implements `IDataverseClient` and injects `IConfidentialClientApplication`, `IHttpClientFactory`, `IOptions<DataverseOptions>`, and `ILogger<DataverseClient>`. Normalize `BaseUrl` to the org root for token scope calculation, then request tokens with `AcquireTokenForClient(new[] { $"{orgRoot}/.default" })`. Use `CreateClient("Dataverse")` for HTTP transport. Build the PluginTraceLog query with `$select=plugintracelogid,typename,messagename,primaryentity,mode,depth,createdon,exceptiondetails,messageblock`, `$filter=exceptiondetails ne null`, optional `createdon gt {utcWatermark}`, and `$orderby=createdon asc`. Follow `@odata.nextLink` until exhausted. On HTTP 429 or 5xx, retry up to 3 total attempts and honor `Retry-After` when present before the next send.</action>
  <verify>rg -n "AcquireTokenForClient|CreateClient\\(\"Dataverse\"\\)|exceptiondetails ne null|@odata.nextLink|Retry-After" src/AIAgents.Core/Services/DataverseClient.cs</verify>
  <acceptance_criteria>
    - src/AIAgents.Core/Services/DataverseClient.cs contains `AcquireTokenForClient`
    - src/AIAgents.Core/Services/DataverseClient.cs contains `CreateClient("Dataverse")`
    - src/AIAgents.Core/Services/DataverseClient.cs contains `exceptiondetails ne null`
    - src/AIAgents.Core/Services/DataverseClient.cs contains `@odata.nextLink`
    - src/AIAgents.Core/Services/DataverseClient.cs contains `Retry-After`
  </acceptance_criteria>
  <done>The Dataverse client can build the required query, authenticate with app credentials, and exhaust paged PluginTraceLog results with bounded retry behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add package reference and Core unit tests for Dataverse behavior</name>
  <files>src/AIAgents.Core/AIAgents.Core.csproj, src/AIAgents.Core.Tests/Services/DataverseClientTests.cs</files>
  <read_first>src/AIAgents.Core/AIAgents.Core.csproj, src/AIAgents.Core.Tests/Services/AIClientTests.cs, src/AIAgents.Functions.Tests/Functions/HealthCheckTests.cs, src/AIAgents.Core/Services/DataverseClient.cs</read_first>
  <action>Add `Microsoft.Identity.Client` to `src/AIAgents.Core/AIAgents.Core.csproj`. Create `src/AIAgents.Core.Tests/Services/DataverseClientTests.cs` with xUnit + Moq tests that cover: query text includes `exceptiondetails ne null`; pagination follows a response that contains `@odata.nextLink`; retry logic waits and retries on 429; and the token scope uses the normalized org root plus `/.default`. Prefer constructor helpers similar to existing service tests and keep all tests free of live Dataverse dependencies.</action>
  <verify>dotnet test src/AIAgents.Core.Tests/AIAgents.Core.Tests.csproj --filter "FullyQualifiedName~DataverseClientTests"</verify>
  <acceptance_criteria>
    - src/AIAgents.Core/AIAgents.Core.csproj contains `PackageReference Include="Microsoft.Identity.Client"`
    - src/AIAgents.Core.Tests/Services/DataverseClientTests.cs contains `public sealed class DataverseClientTests`
    - src/AIAgents.Core.Tests/Services/DataverseClientTests.cs contains `exceptiondetails ne null`
    - `dotnet test src/AIAgents.Core.Tests/AIAgents.Core.Tests.csproj --filter "FullyQualifiedName~DataverseClientTests"` exits 0
  </acceptance_criteria>
  <done>The Core project references MSAL and the Dataverse client behavior is covered by targeted unit tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test src/AIAgents.Core.Tests/AIAgents.Core.Tests.csproj --filter "FullyQualifiedName~DataverseClientTests"` succeeds
- [ ] `dotnet build src/AIAgents.sln` succeeds
- [ ] `src/AIAgents.Core/Services/DataverseClient.cs` contains the PluginTraceLog select/filter/orderby contract from this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dataverse configuration, contracts, and client behavior are implemented in the Core project
- PluginTraceLog query behavior is unit-tested without a live Dataverse dependency
  </success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
